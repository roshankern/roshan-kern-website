/** Timeline pacing: stretches the timeline around acute events (from each script's `acute`). Stub from Task 3; Task 7 implements it. */
import type {Density} from '../../health/warp';

/** Density ranges for the timeline bar, from `SCRIPTS[].acute`, sorted and non-overlapping. Stub: no stretch. */
export function pacing(today:string):Density[]{void today;return [];}
